import { createContext } from "react";

// creatingn a text variable regarding create context for the alert component
const alertContext = createContext();

// exporting it by default
export default alertContext;